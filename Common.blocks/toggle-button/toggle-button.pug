
mixin toggle-button(modifiers, ...argsArr)
    -let modifierNames = '';
    -let args = (argsArr[0] !== undefined) ? argsArr[0] : {};
    -if (args.tag === undefined) args.tag = 'div';
    -if (modifiers === undefined) modifiers = '';
        each val, key in modifiers
            -if (val !== undefined) modifierNames += 'toggle-button_' + key + '_' + val + ' ';
    #{args.tag}.toggle-button&attributes(attributes)(class=modifierNames)
        if block
            block