
mixin toggle-block__descr(modifiers, ...argsArr)
    -let modifierNames = '';
    -let args = (argsArr[0] !== undefined) ? argsArr[0] : {};
    -if (args.tag === undefined) args.tag = 'div';
    -if (modifiers === undefined) modifiers = '';
        each val, key in modifiers
            -if (val !== undefined) modifierNames += 'toggle-block__descr_' + key + '_' + val + ' ';
    #{args.tag}.toggle-block__descr&attributes(attributes)(class=modifierNames)
        if block
            block