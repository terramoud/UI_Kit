mixin container__layout-fonts(args)
    -let modifierNames = '';
    -if (args === undefined) args = '';
        each val, key in args
            -modifierNames += 'container__layout-fonts_' + key + '_' + val
    div.container__layout-fonts&attributes(attributes)(class=modifierNames)
        if block
            block