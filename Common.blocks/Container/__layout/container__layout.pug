mixin container__layout(args)
    -let modifierNames = '';
    -if (args === undefined) args = '';
        each val, key in args
            -modifierNames += 'container__layout_' + key + '_' + val
    div.container__layout&attributes(attributes)(class=modifierNames)
        if block
            block