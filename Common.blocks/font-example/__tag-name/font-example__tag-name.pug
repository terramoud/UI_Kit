mixin font-example__tag-name(modifiers, ...argsArr)
    -let modifierNames = '';
    -let args = (argsArr[0] !== undefined) ? argsArr[0] : {};
    -if (args.tag === undefined) args.tag = 'div';
    -if (modifiers === undefined) modifiers = '';
        each val, key in modifiers
            -if (val !== undefined) modifierNames += 'font-example__tag-name_' + key + '_' + val + ' ';
    #{args.tag}.font-example__tag-name&attributes(attributes)(class=modifierNames)
        if block
            block