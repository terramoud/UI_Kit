
mixin form-elements__toggle-separator(modifiers, ...argsArr)
    -let modifierNames = '';
    -let args = (argsArr[0] !== undefined) ? argsArr[0] : {};
    -if (args.tag === undefined) args.tag = 'div';
    -if (modifiers === undefined) modifiers = '';
        each val, key in modifiers
            -if (val !== undefined) modifierNames += 'form-elements__toggle-separator_' + key + '_' + val + ' ';
    #{args.tag}.form-elements__toggle-separator&attributes(attributes)(class=modifierNames)
        if block
            block