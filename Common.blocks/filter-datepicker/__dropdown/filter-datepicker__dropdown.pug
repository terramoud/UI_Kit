
mixin filter-datepicker__dropdown(modifiers, ...argsArr)
    -let modifierNames = '';
    -let args = (argsArr[0] !== undefined) ? argsArr[0] : {};
    -if (args.tag === undefined) args.tag = 'div';
    -if (modifiers === undefined) modifiers = '';
        each val, key in modifiers
            -if (val !== undefined) modifierNames += 'filter-datepicker__dropdown_' + key + '_' + val + ' ';
    #{args.tag}.filter-datepicker__dropdown&attributes(attributes)(class=modifierNames)
        if block
            block